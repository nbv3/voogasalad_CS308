package objects.events;

// Enum that details the types of events

public enum EEventType {
	CollisionEvent, DamageEvent, PlayerControlEvent, ObjectKillEvent,
	ObjectDespawnEvent, ObjectSpawnEvent, SpawnRequestEvent,
	NULL
}
